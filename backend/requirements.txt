# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4

# ML inference
torch==2.3.1
torchvision==0.18.1
# Continuous (sentence-level) recognition -- served via ONNX Runtime, not
# torch, per the "Render-Friendly Backend" design (see
# continuous_pipeline/export_onnx.py's docstring): no GPU needed at serve
# time. torch above is still required for the ALPHABET classifier's
# inference path (app/ml/inference.py) -- the two models use different
# runtimes by design, not by accident.
onnxruntime==1.18.0
pyyaml==6.0.1
albumentations

# Computer vision
opencv-python-headless==4.9.0.80
# 0.10.33, not the original 0.10.14 pin: continuous_pipeline/landmark_extraction.py
# needs mediapipe.tasks.vision.HolisticLandmarker (Tasks API), confirmed
# present at 0.10.33 -- the exact version this whole project was built and
# tested against in this session. If you pin an older 0.10.x and
# HolisticLandmarker is missing, that's why.
mediapipe==0.10.33

# Model utilities
numpy==1.26.4
pillow==10.3.0

# Database
sqlalchemy==2.0.31
